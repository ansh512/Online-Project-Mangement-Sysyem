<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Webpage with Header, Footer, Sidebar, and Main</title>
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    <style>
      html,
      body {
        height: 100%;
        margin: 0;
        padding: 0;
      }

      .grid-container {
        display: grid;
        grid-template-columns: 1fr 5fr;
        grid-template-rows: 1fr 7fr 0.5fr;
        height: 100%;
      }

      .header {
        grid-column: 1 / span 2;
        background-color: #004cff91;
        color: white;
        padding: 0.5rem;
	   position:relative;
	   align-items: center;
      }

      .sidebar {
        grid-row: 2 / span 1;
        background-color: #f8f9fa;;
        padding: 1rem;
      }

      .main {
        grid-row: 2 / span 1;
        grid-column: 2 / span 1;
        padding: 1rem;
      }

      .footer {
        grid-column: 1 / span 2;
        background-color: #004cff91;
        color: white;
        padding: 1rem;
	   text-align: center;
      }


    form {
      /* margin-top: 20px; */
    }
    .form-group label {
      font-weight: bold;
      margin-bottom: 5px;
      text-transform: uppercase;
    }
    .form-control {
      border-radius: 0;
    }
    .form-control:focus {
      box-shadow: none;
    }
    .form-group small {
      color: #6c757d;
      font-size: 12px;
    }

    /* Style the file upload button */
    .custom-file-label {
      overflow: hidden;
      white-space: nowrap;
      text-overflow: ellipsis;
    }
    .custom-file-label::after {
      content: "Browse";
    }
    .custom-file-input:focus ~ .custom-file-label {
      border-color: #007bff;
      box-shadow: 0 0 0 0.2rem rgba(0, 123, 255, 0.25);
    }

    /* Style the submit button */
    button[type="submit"] {
      background-color: #007bff;
      border-color: #007bff;
      text-transform: uppercase;
      transition: all 0.2s ease-in-out;
    }
    button[type="submit"]:hover {
      background-color: #0069d9;
      border-color: #0062cc;
    }

    /* Style the sidebar */
    #sidebar h3 {
      font-size: 20px;
      margin-bottom: 20px;
      text-transform: uppercase;
    }
    #sidebar ul {
      list-style: none;
      margin: 0;
      padding: 0;
    }
    #sidebar li {
      margin-bottom: 10px;
    }
    #sidebar a {
      color: #343a40;
      text-decoration: none;
    }
    #sidebar a:hover {
      color: #007bff;
    }

    /*style the header*/
    #headTitle{
      position: absolute;
      left: 0;
      padding: 10px;
    }
    #headOptions{
      position: absolute;
      right: 0;
      padding: 10px;
    }
 .hidden {
  display: none;
}

.assignments {
  display: flex;
  flex-wrap: wrap;
  justify-content: space-between;
  align-items: flex-start;
}

.assignment {
  width: 30%;
  padding: 20px;
  margin-bottom: 30px;
  border-radius: 10px;
  box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.645);
  background-color: #ffffff72;
}

.title {
  font-size: 1.5rem;
  font-weight: bold;
  margin-bottom: 10px;
}

.submission-date {
  font-size: 1.2rem;
  margin-bottom: 20px;
}

.download-btn {
  display: inline-block;
  padding: 10px 20px;
  border-radius: 5px;
  background-color: #0066cc;
  color: #fff;
  text-decoration: none;
  transition: all 0.3s ease-in-out;
}

.download-btn:hover {
  background-color: #0052a3;
  transform: translateY(-3px);
}

    </style>
  </head>
  <body>
    <div class="grid-container">
      <div class="header">

<img src="https://w7.pngwing.com/pngs/411/956/png-transparent-customer-login-avatar-head-portrait-client-user-thumbnail.png" alt="User" style="width:50px;height:50px;position:absolute;right:0;margin-right:20px;">
      <h1 id = "headTitle">TU_Labs</h1>


      </div>
      <div class="sidebar">

      <h3>Options</h3>
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link active" href="/login/Instructor/<%=course %>/grade">Submissions</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/login/Instructor/<%=course %>">Post assignments</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="/login/Instructor/<%=course %>/reports">Reports</a>
        </li>

      </div>
      <div class="main">

        <h2><%= course %></h2>
        <h3>Post Assignments</h3>

   <form action="/login/Student/<%=course %>/assignment" method="post" enctype="multipart/form-data">
    <div class="assignments">
        <% assignments.forEach(assignment => { %>
          <div class="assignment">
            <p class="title"><%= assignment.title %></p>
            <p class="submission-date">Submission Date: <%= new Date(assignment.submissionDate).toLocaleString('en-US', { dateStyle: 'medium' }) %></p>
            <a class="download-btn" href="/login/Student/<%=course %>/download/<%= assignment._id%>"> Download</a>
          </div>
        <% }); %>
      </div>      
    </form>

      </div>
      <div class="footer">
          <p>&copy; 2023 anubhavbinit</p>
      </div>
    </div>

  <script>


</script>


</body>
</html>
